# Datenbank
DB_HOST=db
DB_NAME=owianzeiger
DB_USER=owianzeiger
DB_PASSWORD=changeme
DB_ROOT_PASSWORD=changeme-root

# App
NODE_ENV=development
SESSION_SECRET=change-this-to-a-random-64-char-string-before-going-live-!
# Öffentliche Basis-URL für die Magic-Links in Anmelde-E-Mails
APP_URL=http://localhost:3000

# Produktion: auf "production" setzen, um den Caddy-Reverse-Proxy mit
# automatischem HTTPS (Let's Encrypt) zu starten. Leer lassen für lokale
# Entwicklung (dann läuft die App ungesichert auf Port 3000).
COMPOSE_PROFILES=
# Domain, für die Caddy ein Let's-Encrypt-Zertifikat ausstellt.
APP_DOMAIN=owia.treudler.net
# E-Mail für die ACME-/Let's-Encrypt-Registrierung.
ACME_EMAIL=admin@treudler.net

# Adresssuche (OpenStreetMap-Geocoder Photon, intern im Docker-Netz)
PHOTON_URL=http://photon:2322

# Karten-Kacheln (OSM-Raster-Tileserver, intern im Docker-Netz). Die App liefert
# die Kacheln same-origin über /tiles/... aus. Einmaliger Import nötig:
#   docker compose run --rm tileserver import
TILESERVER_URL=http://tileserver:80

# Automatische Foto-Analyse (selbst-gehostet, intern im Docker-Netz). Nach dem
# Upload werden Beweisfotos im Hintergrund analysiert und füllen leere Felder vor.
# Die Dienste alpr + ollama laufen nur im Production-Profil (COMPOSE_PROFILES=
# production); in der Entwicklung ist die Analyse standardmäßig AUS.
# Erzwingen: PHOTO_AI_ENABLED=on (bzw. =off). Default: an in Produktion, aus sonst.
# PHOTO_AI_ENABLED=on
# Kennzeichen-Erkennung (ALPR, fast-alpr):
ALPR_URL=http://alpr:8000
# Verstoßart-Erkennung (Vision-Language-Modell via Ollama). Modell einmalig laden:
#   docker compose exec ollama ollama pull qwen2.5vl:3b
# Bei CPU-Engpass VLM_MODEL=moondream (schneller, einfacher).
VLM_URL=http://ollama:11434
VLM_MODEL=qwen2.5vl:3b
# Kontextfenster fürs VLM (Ollama-Default 4096 ist zu klein für Prompt + Bild).
# Höher = mehr RAM/Zeit auf CPU. Default 16384.
# VLM_NUM_CTX=16384

# E-Mail
# MAIL_DRIVER=mailpit   -> Mailpit (dev, kein Auth)
# MAIL_DRIVER=smtp      -> Echter SMTP (prod)
MAIL_DRIVER=mailpit
MAIL_HOST=mail
MAIL_PORT=1025
MAIL_USER=
MAIL_PASS=
MAIL_FROM=noreply@owianzeiger.local
MAIL_FROM_NAME=OWiA-Anzeiger

# Empfänger-Adresse je Stadt: MAIL_TO_<CITYID> (CITYID wie in src/config/cities.ts).
# Überschreibt den dort hinterlegten Default. Für weitere Städte analog ergänzen.
MAIL_TO_FRANKFURT=ordnungsamt@stadt-frankfurt.de

# "Wir verschicken" (Versand der Anzeige über unser System ans Ordnungsamt).
# Standard: in Produktion AUS, in Entwicklung AN. on/off erzwingt den Zustand.
# SYSTEM_EMAIL_SENDING=on
# ML-Stack exakt gepinnt (Reproduzierbarkeit; Wheels für x86_64 und aarch64).
# torch/torchvision werden im Dockerfile separat vom CPU-Index installiert.
# paddlepaddle NICHT auf 3.2.0 anheben: segfaultet auf aarch64 beim Modell-Laden.
paddlepaddle==3.1.0
paddleocr==3.5.0
ultralytics==8.4.95
huggingface_hub
fastapi
uvicorn[standard]
python-multipart
